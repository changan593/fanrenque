# 变更记录

按时间倒序。只记**规矩、口径、结构与数据**的变化，以及为什么改；逐幕的写作进度在 `git log` 里，不重复。
每条改动都应该能回答「改了什么、为什么、验证了什么」。

---

## 2026-09-05　29 章重审收尾：`s3` 归零

- **数据落地**（用户本机跑，API 已花）：旧版 `--redo-failed` 一轮 33 章过 7 → `s5 --seqs` 修 97 处引用
  （问题条数 83 → 0，T1 归零）→ `--phase review` 22 章过 18 → 最后 4 章（ch0990 / ch1149 / ch1155 / ch1167）
  结构分停在 82、一致性 92~97、引用 100% 逐字，`s5 --waive` 人工放行。聚合层随之重跑
  （`data/characters/`、`data/scenes/`、`data/plot/`）。
- **放行理由补实**：4 章的 `quality.waiver.reason` 落库时还带着模板占位「（填你看到的）」，
  按各章最后一轮结构审查的意见补写（简介字数不足、别名未登记、场景段号区间重叠、beat 概括成省略号等），
  「人读后接受」保留。放行记录是给以后的人看的，占位符等于没写。
- **`s5` 修跨段引用的段号**：ch0350 有一条独白只写了 `para_range: [27, 28]` 没有 `para`——全书唯一一条，
  `s3` 报了段号越界；`check_quote` 对跨段命中只给 `-1`，旧逻辑修不了。`verbatim.locate()` 给出引用起止段，
  `fix_out_of_range_para` 记起始段；`selftest` 加 2 条断言。
- **重审牵动第一季台账**：ch0009 / 0090 / 0103 / 0111 / 0183 重审后关键旁白名单变了，E03 / E22 / E26 / E28 / E44
  里 14 句原先按关键旁白上账的 `【白】`/`【现】` 变成「白描留声」，`s14` 按 `doc/05` 5.4 要 `※` 理由——
  逐句补写（都是叙述者的判词或人物心里的账，画面给不出），47 集的 `旁白承载账.md` 全部重生成（10 集有变化）。
  `check_all --quick` 不跑 `s14`，重审过第一季范围内的章之后要跑完整的 `check_all`。
- **`s14` 报错要看得见**：白描留声只列前 12 条，E22 的 3 处缺理由排在后面，闸门红了却不知道改哪一镜。
  现在缺理由的全列，写了理由的只示例。
- **验证**：`check_all` 全绿（selftest / s3 / s15 / s17 / s18 / s14 × 47）；`s3` 有问题章节 0、T1 / T2 / T3 全 0、人工放行 4。
- 文档：README「已知欠账」、`doc/00` 第三 / 五节、`doc/03` 2.6 / 2.7 / D7 / 待办 11、`doc/10` P3 / 第八节、
  `pipeline/README`、`data/README`、`doc/09` 报告示例改成完成态。

## 2026-09-02　重审收尾的工具补齐

- **`s15` 在 Windows 上全红是路径分隔符**：豁免名单与内容层前缀是 `doc/archive/...` 这种写法，
  而 `str(Path)` 在 Windows 上给的是反斜杠，四个留痕文件的 88 处旧载体词全被当成冲突。
  相对路径统一 `as_posix()`；`selftest` 加一条检查豁免名单的路径都存在。
- **标准输出统一 UTF-8**：`common/__init__` 导入时 reconfigure，`check_all` 按 UTF-8 收子进程；
  Windows 管道下收尾的 `✓` 不再抛 UnicodeEncodeError。
- **`s5` 三件新事**：`--refresh` 只重算质量块；越界段号按正文命中位置改回（只改越界的，范围内标错的不动，
  第一季剧本的 `[n]` 是照 `analysis` 写的）；`--waive / --unwaive` 人工放行。
- **人工放行**（`quality.waiver`）：审查分是模型给的主观分，三轮修订仍卡在 82 而引用早已 100% 逐字的章，
  人读过后可以接受。理由必填、原样入库；`refresh` 只对分数类原因放行，客观项不放；`s3` 单列不算问题。
- 29 章重审的过程记录：旧版 `--redo-failed` 一轮 33 章成功 7；`s5` 免费修掉 83 条引用问题（T1 归零）；
  `--phase review` 22 章过 18；剩 4 章结构分 82、2 处修订轮引入的引用回归、3 章段号越界，都由上面的工具收尾。

## 2026-09-02　合并 E01 开拍准备分支

另一个会话在 `claude/episode-one-prep-review-v8599v` 上做了两次提交（08-28、09-01），一直没进 `main`，
与上面的架构整理并行、同一个基点。本次合并进来，冲突按下面的原则解：

- **他方带来的**：红儿 V6 / 南红枝 V4 / 老拐子 V2 三张卡的提示词与 PNG 重做（正向骨相描述、可量判据、
  残疾的反正常化防线）；新建 `C53_唐假`（身份母版 + Q 版旁白装置 + 等身版，Q 版 PNG 待出）；
  40 张卡的「待生成」改为「已出图，验收待过」并写明验收前图不具备权威；
  新增出图工序规范与出图台账闸门 `s18_render_ledger.py`、`production/s01/E01/出图台账.md`（217 镜次）；
  `s17` 的一批取证引用修正。
- **编号撞了**：他方的 `doc/13_出图工序规范.md` 与本次新建的 `doc/13_剧本格式规范.md` 同号。
  改为 **`doc/16_出图工序规范.md`**，`s18`、台账、README 里的引用同步。
- **冲突解法**：取证引文两边都改了同一批行，取他方的整句版（带句读、`【补】` 标记）；
  C50~C52 三张卡取本次改正的编号 + 他方的状态行；`s17` 用本次的 `cite.py` 版（已覆盖他方的正则修法）；
  `C09_紫云仙宫宫主` 旧卡他方直接删除、本次归档到 `_归档/`，保留归档；
  `s16` 的 `NO_CARD_BY_DESIGN` 按他方移除唐假（已建卡）；README / 路线图 / 交接说明以本次重写版为底，
  把他方的新事实（PNG 现状、三张返工、阶段六进展、写卡两条新规矩）补进去。
- `check_all` 加入 `s18`；`season_roster` 重跑（唐假有卡了）。

## 2026-09-02　架构整理

接手后的一次全面梳理与重构。数据口径、目录结构、脚本、文档四层一起动，
所以单独记一大条。改完的验证：`python pipeline/check_all.py`——selftest（196 条断言）、
`s15`、`s17`、`s14 × 47` 全绿；`s3` 报出 29 章审查分低于合格线，是原本就存在的状态（见「已知未清」）。

### 为什么动

读完全部代码与文档后确认了这些问题，逐项修掉：

1. `data/characters/` 里混着人写的 14 份深度档案与 17 份卷宗，违反「`data/` 全部可重跑」的分界。
2. 脚本互相 import（`s4 → s8`、`s5 → s3`、`s13 → s8`），泛称表两份、文本归一化三套、别名解析两套。
3. 同一个事实写在四处，状态漂移：`doc/00` 说「卡在画风选型」、`doc/03` 说画风已定。
4. 路线图把 47 集的逐集记录塞进表格单元格，最长一格一万多字，表格不可读。
5. `production/s01/` 季级文件两套命名（`00_/02_/03_` 与 `_总则/_视觉基准`）；77 份幕文档引用的 `00_总则与模板.md` 并不存在。
6. 编号冲突：`C09` 有两个目录；E01 剧本与幕文档把余庆写成 `C13`，而补充卷的 `C13` 是双胞胎兄弟；三张龙套卡的元数据还是撞号前的 `C10~C12`。
7. `s17` 不认 `seq2[1][18]` 这种一行挂两段的引用，报的 56 处里 45 处是假阳性；剩下 13 处是真错。
8. `s13` 判「有没有卡」读的是 `02_角色资产.md` 的标题而卡早已搬到目录里，姜羽、周东东、南季礼都被判成没卡；`s11` 把别名表拉平，红儿落到「师姐」头上。
9. `s2 --phase review` 丢掉旧审查记录、调用数从 0 数起；抽取那次调用的用量与降级从不留痕。
10. `s8` 卷宗对外输出 0 基段号；`s2m` 手写稿的 `q` 也是 0 基；全项目其余引用都是 1 基。
11. 668 份被 `s5` 修过的章里 374 份 `coverage` 是修前旧值、31 份 `passed=False` 但逐字早已达标——谁改了 `analysis` 都没有重算质量块。
12. `s10` 重跑会改写 `episodes.json`，而第一季 47 集剧本建立在现有边界上；重构后 timeline 一变，S01 有 16 集边界随之移动。
13. `production/style_test/refs/` 15 个只有 `.gitkeep` 的空目录，采集从未执行；`paths.SEASONS_DIR` 等死项；schema 把模型并不遵守的取值写成枚举。

### 管道 `pipeline/`

**新增**

- `common/names.py`：人物命名的唯一事实来源。`PRESETS`（原在 `s8`，+3：余庆 / 秦怀雀 / 周东东）、
  `RENAME_CANON`、泛称判定（严口径 `is_generic` 管归并，宽口径 `is_unnamed` 管登记）、
  三级别名解析 `build_resolver`（原在 `s13`）、幕文档排行对照 `CARD_ALIASES`（原 `s16` 的 `ROSTER_ALIAS`）。
- `common/cite.py`：`seqN[i]` / `[i-j]` / `[i][j]` 解析、`【原】` 引文提取、省略号分段比对、1 基定位。
- `common/production.py`：`production/characters/`、`scenes/` 卡目录索引；有母版提示词才算建了卡；重号目录检测。
- `common/quality.py`：质量块只有一份实现——`measure` / `gate` / `refresh`，`s2` `s2m` `s3` `s5` 共用。
- `check_all.py`：一键跑全部闸门。

**闸门口径**

- 质量闸门加入**台词覆盖率 ≥ 90%**（原只对人工稿生效）。审查员列出的 `missing_items` **不作闸门**：
  实测 1200 章里 415 章的审查员列了遗漏项，多为「是！」「蜜语」这类噪音，而程序覆盖率全部 100%。
  `p3` 提示词里「非空即不过闸门」的承诺据此改写。
- `s3` 的结论与 `s2` 的 `chapter_state` 一致：审查分低于合格线的章报为 T2，不再被「另有别的问题」这个条件挡住。
  退出码只看 T1 / T2；T3（登记不全、简介短、**段号越界**）只提示。枚举外取值只统计不判错。
- 泛称分两个口径：归并用严口径（并错一次会把不相干的人连成一团），登记用宽口径（「守城甲士」不必登记在册）。
  此前 `s4` 与 `s3` 各一张表、判据不同。

**修的错**

- `s17`：一行多段、区间引用、省略号跨段全部认得；`--fix` / `--relocate` 行为不变。
- `s13`：`has_card` 按 `production/` 卡目录判；排序加名字作 tiebreak，`season_roster.md` 不再每次重跑都变。
- `s11`：改用三级解析，新增 `ambiguous_characters` 字段供人判。
- `s8`：对外 1 基段号；`s2m`：手写稿 `q` 改 1 基，8 份 `manual_analysis/*.json` 已同步 +1，装配结果逐字节不变。
- `s2`：复审在原记录上追加，旧 `reviews` / `llm_calls` / `repair_rounds` 保留；抽取调用留 `extract_meta` 记录；
  `--range / --seqs / --smoke` 互斥；`BASE_STEPS` 5 步（含写入）；退出码 0 / 1 / 130。
- `s10`：**已有 `剧本.md` 的集边界锁定**，只重排其余部分；集号会被重编时直接报错。
- `s12`：`change_curve` 里权重变量遮住了窗口参数（重构时引入，跑一次就崩），已改名。
- `s9`：`char_count` 取 `source.char_count`（原取 `raw_text` 长度，含空白）；不再生成 `digest_all.md`（34.9 万字，无人读）。
- `s16`：场景码正则不吃前缀字母、三位数与集号；`--list` 跳过下划线目录；`NO_CARD_BY_DESIGN` 加老赵。
- `s5`：补登人物的 `role_in_chapter` 用 schema 里的「参与」，不再写自造的「出场」；修完调 `quality.refresh`。
- `jsonio.write_json`：临时文件按 umask 放回常规权限，重生成的 json 不再变成 0600。
- `verbatim.coverage_report`：拼接顺序按列表而非 set，结果可复现。
- `llm.py`：调用日志改名 `llm_calls.jsonl`（不只 `s2` 在用）。
- 全部脚本 `main() -> int` + `sys.exit(main())`；魔法数进 `config.py`（s3 / s4 / s9 / s10 / s11 / s12 / s13 / s14 各一节）。
- `selftest.py`：140+ → 196 条断言，新增第 13 节（公共模块）、复审记账、剧本锁、Pillow 缺失时跳过裁图而非崩溃；
  去掉对 `Path.exists` 的猴子补丁，临时文件全部进 `TemporaryDirectory`。

### 数据 `data/`

- **章节 json**：745 份有变化——286 条 `role_in_chapter` 「出场」→「参与」；632 章 `coverage` 重算
  （值都是 100%，变的是拼接口径与遗漏样本）；4 章 `passed` 由 False 翻成 True（旧 `fail_reasons` 是 s5 修复前的）。
  `verbatim` 一处未变。29 章仍 `passed=False`（见「已知未清」）。
- **角色索引重建**：973 → 997 个。秦淮雀（38 次）并入秦怀雀、紫云峰余庆并入余庆；26 个泛称
  （宫人、将军、甲士、黑狗……）不再被并进具名角色——此前它们经由错别名把不相干的章挂到了主角名下
  （唐真 636 → 634 章：seq389「凡人／围观百姓」、seq1183「仙师」；姚安饶 300 → 299：seq244「最能歌善舞的姑娘」）。
- **卷段** 88 → 90（timeline 的人物列改用三级解析后变更分变了）；**分集** 268 → 267（S04 46 → 45，S06 三集边界微调）；
  **S01 47 集边界锁定不变**。`timeline.json` / `episode_assets.json` / `season_roster.*` / `cooccurrence.json` / `digest_p0*.md` 随之重建。
- 卷宗 17 份 `.md` 迁到 `data/dossiers/`，`.json` 不再入库（秒级可重跑，没有脚本消费）；重跑后 `.md` 逐字节不变。
- 新建 `data/manual/`：`seasons.json`、`arcs_manual_p02.md`、`threads.json`、`structure_signals.json` 迁入——它们是人定的输入或一次性分析产物，不是脚本产出。
- 删除 `digest_all.md`；`quality_report.json` / `rerun_seqs.txt` 改写到 `.run/reports/`（每次重写的报告不入库）。
- `text_probe.json` 重跑（此前是过期版本）。

### 产出 `production/`

- 14 份深度档案迁到 `production/characters/_深度档案/`；`C09_紫云仙宫宫主/`（V1 下半脸卡）归档到 `_归档/`，`C09` 只剩南季礼一个目录。
- `_总则与模板.md` → `01_总则与模板.md`，`_视觉基准_E03-E10补充.md` → `04_视觉基准_E03-E10补充.md`；77 份幕文档、README、doc 里的引用同步。
- 13 处 `【原】` 引用修正（`s17` 现全绿）：C15 卡「对着」；`02_角色资产` 576 行补「她」、664 行「带着自家儿子」补 `seq3[2]`；
  `03_场景资产` 119 行去「个」、264 行改为原句「朱红色的马车…高壮的白马，精雕的车厢」；
  `04_视觉基准` 2623 行恢复「园林风格格不入」全句、3660/3661 行 `seq39 → seq38`、3905 行 `seq35 → seq39`、
  4193/4225/4254 行三处非引文改用 `『』`、4218 行把「（按裁定出刀）」移出引号。
- E01 剧本与冷开场幕文档里余庆的编号 `C13` → `C49`；C50~C52 三张卡的 `character_id` / `asset_id` 由撞号前的 `C10~C12` 改正；
  `02_角色资产.md` 的 `# C09 紫云仙宫宫主` 与 `04_视觉基准` 5.6 标题改为南季礼。
- 47 集剧本抬头去掉「（草稿）」；「画幅/画风：待 doc/04 第十四节定稿」改为已定的 16:9 与 `style_assets`。
- `style_test/refs/` 15 个空占位目录删除，README 顶部标明「未执行」。

### 文档

- 新增 `doc/13_剧本格式规范.md`（剧本 / 幕文档 / 承载账的格式与标记，`s14` `s16` 按它解析）、
  `doc/14_术语与编号表.md`（术语与四套编号）、`doc/15_写作台账.md`（从 `doc/03` 抽出的逐集抽取误判、闸门演进、时长实测）。
- `doc/06`、`07`、`12` 归档到 `doc/archive/`（选型已结束，`s15` 的留痕豁免路径同步）。
- 根 `README`、`doc/README`、`doc/00`、`doc/03`、`doc/10` 重写；`doc/01` `02` `04` `05` `08` `09` `11`、
  `pipeline/README`、`data/README`、`production/**/README`、`production/s01/00`、`01` 更新到现状。数字统一以 `doc/00` 第五节为出处。
- 新增本文件。`.gitignore` 加 `.claude/`、`*.tmp`、`data/dossiers/*.json`；非空目录里的 `.gitkeep` 删除。

### 考虑过但没做

- 不从章节 json 里剥掉 `raw_text` / `paragraphs`：它是「分析时所见文本」的快照，补原文后 seq 会变，快照才能对上分析。
- 不重写 git 历史、不引入 Git LFS：PNG 都在历史里了，改写历史对协作方成本太高，只定新图入库的规矩。
- 不把 `04_视觉基准` 拆成逐实体的卡目录：38 角色 + 39 场景 + 41 道具大多尚无出图卡，等某实体建卡时再把它那一节搬过去。
- 不动 47 集剧本的正文，只修编号与抬头。
- 不把 29 章重审做掉：要花 API，且要不要花由用户拍板（`doc/03` D7）。

### 已知未清

- ~~29 章审查分低于合格线（`s3` T2），需 `s2 --redo-failed`；3 章有段号越界的引用（ch0350 / ch0670 / ch1141，T3）。~~ 09-05 已清，见上。
- 第 1115~1205 章之间 41 章正文被截（`doc/11`）。
- 卷段是算法版；第一季只有 E01 有分镜草图。

---

## 2026-08 及以前（按提交记录整理的决策要点）

- **08-27** E01 全部角色卡与场景卡落地（15 具名 + 5 群像 / 10 场景）。
- **08-25** 南季礼更名裁定 B：seq15[4]「南天礼」为全书唯一异名，成片一律读南季礼，证据层不动；
  红儿 × 南红枝改按眉眼同源锁重做；清洗全季 V1 残脸；新增原文引用闸门 `s17`，查出 `s8` 卷宗 0 基段号的问题。
- **08-24** 画风定稿**国风三维**，`production/style_assets/` 两份统一提示词成为唯一来源；画风金标准做成闸门 `s15`，清掉 34 处载体冲突；
  `production/` 重构为季 → 集 → 幕三级，场景改母版 + 状态；新增按幕装配脚本 `s16`；恢复误删的 `source/`。
- **08-19** 分镜提示词总则与模板；E03~E10 视觉基准补充卷（C11~C48 / S15~S53 / P01~P41）。
- **08-11 ~ 08-15** E08 ~ E47 剧本声音层完成，第一季 47 集全部写完；`s14` 先后补六道机械检查（记录在 `doc/15` 第二节）。
- **更早（记录在文档里）**：每集时长规格两次作废，最终决定不做预设；不达标章节改由 `s5` 程序化对齐而不重跑 API；
  别名归并的闸门建在簇上而非成对判断（一条错别名曾把 973 个角色塌成一个「唐真」）；
  `LLM_THINKING=0` 成为默认（500 章跑批只成功 9 章的教训）；逐字率合格线放宽到 95%，臆造 = 0 不放宽。
