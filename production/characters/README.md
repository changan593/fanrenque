# production/characters —— 跨季人物视觉资产

已批准的**角色卡图片**与**超详细提示词**，一角色一个目录，跨全六季通用。

**为什么在 `production/` 不在 `data/`**：`data/characters/` 是 `s4_build_assets.py` 从逐章分析
算出来的（973 个 json，重跑就能再生，且重跑时会 `glob("c*.json")` 清旧文件）；
本目录是人工出图、人工裁定、删了就没了的东西。分界线见根 [`../../README.md`](../../README.md)。

**为什么不在 `s01/` 下**：唐真出现在全部六季，身份母版不是第一季的资产。
只属于某一季的东西（分集方案、剧本、当季新增实体的锁定卡）才放 `s01/`~`s06/`。

---

## 目录结构

```text
production/characters/
└── C01_唐真/
    ├── 00_身份母版/          ★ 跨期不变项的最高权威
    │   ├── 唐真_身份母版_角色卡.png
    │   └── 唐真_身份母版_超详细提示词.md
    ├── 01_真君期/            约 20 岁，青云榜榜首，被废修为之前
    ├── 02_乞丐期/            约 22 岁，北阳城城隍庙，黑印外露
    └── 03_白色抹额期/        ch0047 起，抹额遮印、系统重启
```

- 目录名用 `CNN_角色名`，编号与 [`../s01/02_角色资产.md`](../s01/02_角色资产.md) 的角色编号一致
  （`s13_season_roster.py` 也是按 `C` 编号扫"有没有卡"的）。
- 阶段目录用 `NN_阶段名`，`00` 恒为身份母版，`01` 起按**时间顺序**排。
- 每个阶段目录里放一张 `角色卡.png` 和一份 `超详细提示词.md`，文件名带角色名，防止跨目录复制后认不出。

---

## 身份优先级

出图时任何两条冲突，按这个顺序服从：

```
原文章节证据
> 已批准身份母版图片
> 已批准阶段角色卡图片
> 角色深度档案与阶段提示词
> 当前镜头的动作、表情和光照要求
> 统一角色提示词
> 通用场景风格与模型默认审美
```

**身份母版只决定跨时期不变项。** 黑印、抹额、伤势、胡须、服装、桃枝这类阶段变量
必须由阶段卡决定——不能因为某张参考图里出现过就永久继承。

---

## 制作顺序

1. 先出**身份母版**，人工验收通过（验收表在每份母版提示词末尾）
2. 由母版**派生阶段卡**，脸完全继承，只改该阶段允许变化项
3. 由阶段卡**进入剧情镜头**，只改姿势、表情、视线、衣物受力、局部污损和场景光照

**绝不能反过来**：不要从不同剧情截图反向平均出一张新脸。

提示词怎么拼见 [`../style_assets/README.md`](../style_assets/README.md)。

---

## 阶段命名对照

`02_角色资产.md` 写于 E01/E02 试点期，用的是旧的两段划分。以现在的三段为准：

| 旧称（`02_角色资产.md`） | 现称（本目录） | 时期 |
| --- | --- | --- |
| 仙宫期 | **01_真君期** | 约 20 岁，紫云道袍，无黑印、腰间无桃枝 |
| 凡尘期 | **02_乞丐期** | 约 22 岁，黑印外露，破烂，腰间干枯桃木枝 |
| （无） | **03_白色抹额期** | ch0047 起，白抹额遮印、白道袍、桃枝保留 |

旧称还散落在 `02_角色资产.md` 与部分剧本里，**读到旧称按上表换算**，不要新建第三套词。

---

## 现状

| 角色 | 身份母版 | 阶段卡 | 备注 |
| --- | --- | --- | --- |
| C01 唐真 | ✅ | 3 个 | 第一季 203 个角色里的第 1 个 |

工作队列见 [`../../data/plot/season_roster.md`](../../data/plot/season_roster.md)：
第一季 203 角色（主要 25 / 次要 31 / 龙套 147），主要 25 个是优先项。
