# 项目总纲

把《穿越后，系统变成白噪音了怎么办》改编成 AI 漫剧。

## 一、三大原则

这三条高于一切。任何环节的任何决策与它们冲突，改决策，不改原则。

### 原则一：最终目标是高质量成片

所有环节都要谨慎细致，人物、场景保持高一致性。
宁可某一步多花时间、多花成本，也不把问题往下游传——
上游一个错误的人物设定，到成片阶段就是几十集的返工。

**落地要求**：每个环节都要有可验证的质量闸门，不能靠「看着还行」放行。
现在有六道：`selftest`（管道逻辑）、`s3`（章节分析）、`s14`（剧本旁白承载）、
`s15`（画风金标准）、`s17`（原文引用）、`s18`（出图台账）。`pipeline/check_all.py` 一次跑完。

### 原则二：严格遵守原文

- 原文有多少人物、多少场景，就多少人物、多少场景，不增不减
- 原文怎么描写人物和场景，就怎么呈现，不做二次创作
- 人物对话、心理活动、关键旁白，一个都不能漏
- 不允许为了「戏剧效果」加原文没有的情节、台词、动作

**落地要求**：逐章分析里所有引用必须逐字来自原文，由程序做字符串精确匹配核验，
不依赖模型自觉。详见 `doc/02_章节分析规范.md`。
人写的角色卡、场景卡、幕文档里的引用同样受查：`【原】「…」` 后面标的 `seqN[i]`
由 `s17` 逐字核对，段号一律 1 基。

> **「关键旁白」是有定义的**：`pipeline/prompts/p1_extract.txt` 与
> `pipeline/schemas/chapter.schema.json` 都写明——`narration` 只收
> 「交代设定、背景、时间推移、伏笔、重要情节」的旁白，
> **纯环境白描归 `scenes.description_quotes`，不许塞进 `narration`**。
> 所以受本条保护的就是 `analysis.narration` 那一份，E01 是 29 条。
>
> 另外两件事在 `doc/05_唐假旁白系统.md` 第五节定了口径：
>
> 1. **「不漏」不等于「必须出声」。** 不漏的是信息，不是声音。
>    摄影机拍得出来的（眼前动作、环境气氛、时间推移）改由画面承载，
>    只要上了承载账并写明镜号，就不算漏。
> 2. **剧本额外配音的白描该归谁管。** E01 实测：关键旁白 29 条，
>    而剧本给唐假的曾是 56 条——多出的 31 条是白描，
>    按新口径它们绝大多数归画面；仍派给唐假的必须写保留理由。
>
> 在这个前提下才有第三档：关键旁白里「信息已被同集别处覆盖**且**不是招牌句」的可删，
> 两条要同时成立，且必须上账、写明被谁覆盖、经你点头。
> 闸门由 `s14_narration_ledger.py` 程序化检查，格式见 `doc/13_剧本格式规范.md`。
> **对话与心理活动没有任何口子，仍是一条不漏。**

### 原则三：画面风格朴素真实

场景和人物都要朴素真实，**严禁浮夸**。

这本书的底色是市井、落魄、克制的——开篮主角是破庙里的小乞丐，
老拐子的幸福想象是「天天吃羊下水」。美术风格必须接得住这种质感，
不能做成流光溢彩的网游 CG。

**落地要求**：风格规范见 `doc/04_风格规范.md`；画风载体已定为国风三维，
渲染段与人物塑造段的唯一来源是 `production/style_assets/`，
所有人物、场景资产的提示词都要受它约束，`s15` 逐字查。

**关键认识**：「严禁浮夸」不是外加的审美偏好，而是**原著自带的美学自觉**——
作者在书里就借角色之口嘲讽「喷火啊、放电啊」这类术法是「街边杂耍」。
全书 268 万字里「闪耀」只出现 1 次、「璀璨」16 次，而「脏」有 152 次、「旧」526 次。
所以原则二与原则三在这本书上是同一件事：忠于原文，自然就不浮夸。

## 二、规格

| 项 | 值 |
| --- | --- |
| 原著 | 1200 个分析单元，正文 2,680,694 字（详见 `doc/01_原文数据说明.md`） |
| 每集 | 2~5 章，**时长不做预设**（原定 8-12 分钟、后改 25~32 分钟，均已作废，见下） |
| 总集数 | **267 集**（求解结果，见 `doc/08_全书剧情线.md`；第一季 47 集的边界已随剧本锁定） |
| 季数 | 6 季，41~47 集/季 |
| 画幅 / 画风 | 16:9 横屏 / 国风三维（`doc/04` 3.1、第十四节） |

**集数是算出来的，不是拍的。** 分集不平均切：先按时长模型
（语音字数 ÷ 4.5 字/秒 × 1.20）估每章时长，再用最短路求解器在
「每集 25~32 分钟」和「尽量收在剧情断点上」之间取最优，
所以每集章数在 2~5 之间浮动。下限必须放到 2——
seq655~659 这样的长章连着三章就已经 42~44 分钟了。

**关于时长**：E01-E02 试点实测，4 章一集若要满足原则二（对话、心理活动、
关键旁白一个都不能漏），纯语音就需 28~30 分钟，与原定的 8-12 分钟差近 3 倍。
于是先把规格改成 25~32 分钟。**但写到 E06 时这条规格也作废了。**
逐镜配秒下来，全季 47 集从 19:41 到 162:10，散得很开，方向也不一致
（逐集数字见 `doc/15` 第三节）。原因是「一集多长」根本不是可以先定后写的东西：
它是「这一集有多少内容必须落地」的结果。**已决定不对时长做预设，专注内容。**

所以分集边界的 `minutes` 字段只作**求解器的排布依据**，不作成片验收标准；
`s14` 仍会报出每集的逐镜秒数合计，但那是**报告，不是闸门**。

**有剧本的集，边界不再变。** 分集表是求解出来的，时长参数、卷段、章节数据任何一样变了边界都会动；
但剧本是按边界写的。所以 `s10` 会把已有 `剧本.md` 的集原样锁定，只重排其余部分。

## 三、六个阶段

```
① 原文标准化          txt ──→ novel.json                          【已完成】
② 逐章独立分析        每章一个 json，含审查留痕                    【已完成 1200/1200；29 章待重审】
③ 资产与结构规划      角色资产、场景资产、全书剧情线、六季分集      【已完成】
④ 风格定稿与资产深化  画风已定；角色卡 / 场景卡逐个做              ◀ 进行中，是量的活
⑤ 第一季深化          47 集剧本声音层已完成；画面提示词层写到 E10   ◀ 进行中
⑥ 第一季制作          E01 卡图验收中；出图工序与台账已定（doc/16）      ◀ 验收通过 0，通过前不开工
```

### 阶段一：原文标准化 ✅

`pipeline/s1_normalize_novel.py` → `source/novel.json`

已完成。产物与已知缺陷见 `doc/01_原文数据说明.md`。
**其中第三部分：第 1115~1205 章之间有 41 章正文被截，缺约 5.0 万字，
影响第六季，需要补原文。** 待补章号清单见 `doc/11_待补原文清单.md`。

### 阶段二：逐章独立分析 ✅

`pipeline/s2_analyze_chapters.py` → `data/chapters/chXXXX.json`

每章独立分析，互不干扰。每章固定三次模型调用（抽取 + 结构审查 + 一致性审查），
中间夹一道程序逐字核验，不达标自动触发修订轮。
审查记录（分数 + 详细分析）全部留在章节 json 里。规范见 `doc/02_章节分析规范.md`。

已跑完 1200/1200。体检结果：**逐字命中率 100%、台词覆盖率 100%、臆造 0 条**。
最后一段差距（31 章共 91 条引用不逐字）是用 `pipeline/s5_repair_quotes.py`
程序化对齐掉的，没有再花 API 的钱——那 91 条经逐条比对全部是机械性错误
（跨段、拼接、混入旁白、顺手改通顺），没有一条是真的凭空编造。

**尚欠一笔**：29 章在修订轮用尽后审查分仍低于合格线（结构 < 85 或一致性 < 90），
`s2` 记为 failed、`s3` 列为 T2。引用本身没问题，问题在模型审查员的评分。
重跑要花 API：`python pipeline/s2_analyze_chapters.py --redo-failed`。

### 阶段三：资产与结构规划 ✅

有了 1200 份章节分析，才谈得上全局判断。产出：

- `data/characters/` —— 997 个角色资产；人写的 14 份深度档案在 `production/characters/_深度档案/`
- `data/scenes/` —— 1364 个场景资产
- `data/plot/` —— 全书剧情线、90 个卷段、267 集分集表、每集资产需求、按季清单
- `data/manual/` —— 六季定义（人定）、seq201~400 的人工卷段、十一条长线与地理迁移的一次性分析

### 阶段四：风格定稿与资产深化 ◀ 进行中

**画风载体已定稿：国风三维**（`doc/04` 3.1）。落地资产是 `production/style_assets/` 的两份统一提示词。

剩下的是量：把角色、场景资产深化到能直接出图的程度（身份母版 + 状态卡），确保跨集一致性。
E01 涉及的 16 个具名角色（含 C53 唐假）、5 组群像、10 个场景已全覆盖，PNG 已生成、人工验收通过 0；第一季共 207 角色、274 场景，
工作队列在 `data/plot/season_roster.md`。做法与纪律见 `production/characters/README.md`、
`production/scenes/README.md`。

`doc/04` 第十四节的待定项 D（节奏基准）、E（分层节奏）、F（配音）仍待拍板，不阻塞出图。

### 阶段五：第一季深化 ◀ 进行中

47 集分镜脚本的**声音层**（台词 / 独白 / 旁白分派、镜号、景别、时长、表演提示）全部完成：
485 幕、12,958 镜，`s14` 五道闸门全绿。**画面提示词层**（每幕一份 `提示词.md`）写到 E10，共 77 幕。
分镜草图只有 E01。写作中逐集查出的抽取误判见 `doc/15`。

### 阶段六：第一季制作 ◀ E01 具备开工条件，卡在卡图验收

出图的工序、命名、台账、三层验收判据与返工决策树在 `doc/16_出图工序规范.md`，闸门 `s18_render_ledger.py`；
每集入库的是 `出图台账.md`，镜头成品图不入库。E01 的 20 张角色卡、23 张场景卡 PNG 已出，**人工验收通过 0，通过前不许开工**。
已查出三张不过并重写了提示词：红儿 V6、南红枝 V4（脸偏圆、缺特色）、老拐子 V2（右脚未做出萎缩内翻）。
出片（图 → 视频）与配音后议：先做好每一张图。

## 四、目录与文档索引

- 目录结构与「我想知道 X 该看哪份文档」的对照表 → `README.md`
- 每个文件、每个 JSON 字段的含义 → `doc/09_数据字典.md`；术语与编号 → `doc/14_术语与编号表.md`
- 换人 / 换会话接手 → `doc/10_交接说明.md`
- 逐项待办与阻塞状态 → `doc/03_路线图.md`
- 变更与决策记录 → `CHANGELOG.md`

## 五、当前状态

**这张表是全项目数字的出处**，其他文档引用它，不各自复述。

| 事项 | 状态 |
| --- | --- |
| 原文标准化 | ✅ 1200 个分析单元，2,680,694 字 |
| 逐章分析 | ✅ 1200/1200，逐字 100% / 台词覆盖 100% / 臆造 0；⚠ 29 章审查分低于合格线待 `--redo-failed` |
| 角色与场景资产 | ✅ 997 角色 · 1364 场景 · 3000 对同框关系 |
| 角色卷宗 / 深度档案 | ✅ 17 份卷宗（`data/dossiers/`）· 14 份深度档案（`production/characters/_深度档案/`） |
| 全书剧情线 | ✅ `doc/08`：地理六幕、阵容曲线、十一条长线 |
| 卷段 | ⚠ 算法版 90 段，只约六成站得住，仅作软约束；人工版只有 seq201~400 |
| 六季划分与分集表 | ✅ 6 季 267 集（47/46/42/45/41/46），第一季 47 集边界已锁定 |
| 画风载体 | ✅ 国风三维；`s15` 全绿 |
| 角色卡 / 场景卡 | ⏳ 16 具名（含 C53 唐假）+ 5 群像 / 10 场景（E01 全覆盖）；PNG 已出 15 具名 + 5 群像 + 全部场景，人工验收通过 0；第一季 207 角色 / 274 场景 |
| 第一季剧本 | ✅ 47 集声音层，485 幕 12,958 镜，`s14` 全绿；⏳ 画面提示词层 77 幕（E01~E10） |
| 原文引用闸门 | ✅ `s17` 1449 条引用全部逐字命中 |
| 第 1115~1205 章之间 41 章被截 | ⚠ 缺约 5.0 万字，影响第六季，清单见 `doc/11` |
