# 路线图与阻塞状态

全部待办一次列清，并标明**哪些卡在逐章分析上、哪些不卡**。

状态记号：✅ 已完成 · 🔧 工具就绪待数据 · ⏳ 进行中 · ⛔ 被阻塞 · ⚠ 待你决策

---

## 阶段一　原文标准化 ✅

| # | 事项 | 状态 |
| --- | --- | --- |
| 1.1 | txt → `source/novel.json`（1200 个分析单元） | ✅ |
| 1.2 | 章节编号异常排查（重号/跳号/合并单元） | ✅ |
| 1.3 | 数据缺陷成文 `doc/01` | ✅ |

## 阶段二　逐章独立分析 ✅

| # | 事项 | 状态 |
| --- | --- | --- |
| 2.0 | **修复思考模式默认开启导致的批量失败**（500 章只成功 9 章） | ✅ `LLM_THINKING=0` + 自适应降级重试 |
| 2.1 | 跑全书 1200 章（本地 deepseek-v4-flash） | ✅ 1200/1200 完成 |
| 2.2 | `s3_validate_chapters.py` 全量体检 | ✅ 逐字 100% / 台词覆盖 100% / 0 问题章 |
| 2.3 | 人工抽检，逐字比对原文 | ✅ 全书 566 条修复片段全量复核：0 条非原文子串、0 条缩水超 30% |
| 2.4 | `--phase review --force` 补一轮完整上下文审查 | ⚠ 未做，需花一次审查的钱；当前分数已达标，性价比低 |
| 2.5 | ~~不达标章节重跑~~ | ✅ 改为 `s5_repair_quotes.py` 程序化对齐修复，未花 API |

## 阶段三　资产与结构规划 ✅（3.6 / 3.10 除外）

| # | 事项 | 状态 |
| --- | --- | --- |
| 3.1 | 角色资产聚合 | ✅ 973 角色 |
| 3.2 | 场景资产聚合 | ✅ 1364 场景 |
| 3.3 | 角色同框关系网 | ✅ 3000 对 |
| 3.4 | 纯原文结构探查（角色池雏形、篇幅曲线、卷段边界候选） | ✅ `s0_probe_text.py` 已跑出 |
| 3.5 | 全书剧情线 | ✅ `doc/08`：地理六幕、阵容曲线、十一条长线 |
| 3.6 | 伏笔埋设与回收对照表 | 🔧 `threads.json` 已出 11 条长线；章级 2400 条待细排 |
| 3.7 | 六季划分方案 | ✅ 47/46/42/46/41/46 集，极差 5 |
| 3.8 | 分集表 | ✅ 268 集，`data/plot/episodes.json` |
| 3.9 | 分集规划脚本 | ✅ `s10_episode_plan.py` 最短路求解 |
| 3.10 | 卷段检测 | ⚠ `s12_detect_arcs.py` 算法版已出 88 段，但只六成站得住，仅作软约束；建议额度恢复后人工重做。另：原文自带的两处卷末记号（ch62、ch254）都落在集的正中间，已决定不为它们调整分集边界，第一处按原样做成字幕卡保留在 `E16` 第 81 镜，理由见 `E16` 附二 |
| 3.11 | 每集资产需求表 | ⚠ `s11_episode_assets.py` → `episode_assets.json`。场景列可用，**角色列归属有缺陷**（把 `红儿` 算成 `师姐` 而非 `姚望舒`），见 `doc/10` 5.6 |
| 3.13 | 按季资产清单 | ✅ `s13_season_roster.py` → `season_roster.json/.md`。归属判定分三级，是 4.2/4.3 的工作队列 |
| 3.12 | 主要角色深度档案 | ✅ 14 份，`s8_character_dossier.py` 取证后逐字撰写 |

## 阶段四　风格定稿与资产深化 ⏳ ← 当前

| # | 事项 | 状态 |
| --- | --- | --- |
| 4.0 | **画风载体选型** | 🔴 **全项目唯一阻塞点**，选型矩阵已就绪，`s6` → `s7` 跑完拍板 |
| 4.1 | 风格规范：画风、人物、动作、打斗、对话、场景、观感、转场、特效 | ✅ 草案已出 `doc/04`，含 6 个待你拍板的待定项 |
| 4.2 | 角色资产 → 可直接出图的超详细提示词 | ⛔ 需 3.1 + 4.1。**工作队列已就绪**：第一季 203 角色（主要 25 / 次要 31 / 龙套 147），见 `data/plot/season_roster.md` |
| 4.3 | 场景资产 → 同上 | ⛔ 需 3.2 + 4.1。同上：第一季 274 场景，其中 ≥3 章的 22 个是重点 |
| 4.4 | 跨集一致性约束文档（锁定项清单） | 🔨 框架可先写 |
| 4.5 | 一致性检查工具（境界/时间线/称呼漂移） | 🔨 可先写 |

## 阶段五　第一季深化

| # | 事项 | 状态 |
| --- | --- | --- |
| 5.1 | 第一季剧情线细化 | 🔨 **不阻塞**，`data/plot/digest_p01.md` 就是原材料 |
| 5.2 | 每集详细安排（2~5 章一集） | ✅ `s10` 已求解，47 集，见 `episodes.json`。其 `minutes` 只是求解器排边界用的估值，非验收标准（见 5.6） |
| 5.3a | 分镜脚本 · **声音层**（台词/独白/旁白分派、镜号、景别、时长、表演提示） | ⏳ **E01~E36 完成**（36/47），五道闸门全绿。E37 起继续 |
| 5.3b | 分镜脚本 · **画面提示词层** | ⛔ 需 4.0 画风 |
| 5.4 | 剧集脚本的数据结构与模板 | ✅ 试点已定型，见 `production/s01/E01_剧本.md` |
| 5.5 | **旁白六类分派**（N1~N6）与承载账 | ✅ 规范已定（`doc/05` 第五节），闸门就绪（`s14`），E01~E19 全部合规。世界静止：E01 5、E02 6、E03 3、E04 4、E05 3、E06 3、E07 3、E08 3、E09 3、E10 4、E11 4、E12 3、E13 4、E14 5、E15 5、E16 4、E17 5、E18 3、E19 5、E20 **6（首次用满上限）**、E21 5、E22 5、E23 **6（第二次用满）**、E24 **6（第三次用满）**、E25 **6（第四次用满）**、E26 **6（第五次用满）**、E27 **6（第六次用满）**、E28 **6（第七次用满）**、E29 **6（第八次用满）**、E30 **6（第九次用满）**、E31 **6（第十次用满）**、E32 **6（第十一次用满）**、E33 **6（第十二次用满）**、E34 **6（第十三次用满）**、E35 **6（第十四次用满，且六次全是正条——全季第一次不需要给任何一次现身档写 `※`）**、E36 **6（第十五次用满，六次同样全是正条）**（均 ≤6）。`s14` 又补了两道机械检查：**跨 seq 撞段号**（段号只记 `para` 不记 `seq`，E13 镜083 是 seq51[35] 却差点算成 seq50[35] 的拆条，现在按镜头正文回查它属于哪一章）与**逐字回查全部十三集**（查出 E04/E05/E06/E09 各有一处 `【画】` 把原文的人名换成了代词，已改回）。两项都进了 `selftest` 第 12 节。E14 又补了第三道：**附表不参与比对**（附表行的首格也是三位数字，正文里再出现一个 `【白】` 就会被当成真标记，E14 的附四正是这样多出过一条没写 `※` 的白描留声）。这一改顺带查出 E01 镜179、E02 镜013 两处归属句只写在表演提示栏里，已挪回声音栏。E16 又补了第四道：**镜头归章按前后邻居收窄与补空**（`【删】。。。[39]` 这种镜头正文认不出属于哪一章，两章都有第 39 段就会互相撞；镜头是按叙事顺序排的，前后两镜同属一章就够判了）。E17 又补了第五道：**正向包含也要排除「整条正文就是另一条旁白」**（seq68[49]「朽木不可雕也。」整个嵌在 seq68[57]「实在是朽木不可雕也。」里，此前反向包含挡得住、正向包含挡不住）。**E34 又补了第六道：正向包含也要查段号与所属章**——E17 加的 `others` 只挡得住「正文整条等于另一条**旁白**」，挡不住「正文整条等于另一条**心理活动**、而那条心理活动又原样引用了本条旁白」：E34 `seq137[90]`「一个大巴掌。」整个嵌在 `seq137[93]`「其实不是一个大巴掌，更像是扔开什么垃圾一样。」里，后者是 `monologues` 不是 `narration`，于是 [90] 被算到了两个镜头上。承载某条旁白的镜头一定写着那条旁白的段号，所以正向包含分支加查 `tag and same_seq` 即可。**这一改顺带查出两处旧账**：E01 镜193 的段号写错（正文是 `seq4[49]`，剧本写 `[50]`，此前靠正向包含蒙混过关，还害它被当成「没写理由的白描留声」）、E03 镜034 的 `【台】` 片段漏写段号 `[37]`，两处均已改。改完 E01~E33 全部重跑，闸门无一条不合格；两条新用例已进 `selftest` 第 12 节 |
| 5.6 | ~~逐镜秒数重配~~ | ✅ **已定：不对时长做预设，专注内容。** 三十六集实测 19:41 / 25:33 / 25:40 / 41:29 / 38:38 / 48:34 / 54:14 / 40:37 / 54:58 / 61:37 / 67:01 / 55:18 / 61:53 / 61:24 / 40:40 / 57:50 / 73:23 / 46:34 / 81:36 / 79:28 / 60:57 / 65:00 / 72:32 / 70:40 / 71:44 / 65:16 / 84:34 / 71:00 / 87:38 / 85:46 / 83:18 / 95:22 / 115:34 / 116:28 / 138:14（**E35，全季最长**）/ 92:34，散得很开——因为「一集多长」是内容量的结果，不是可以先定后写的指标。`doc/00` 的 25~32 分规格随之作废，`episodes.json` 的 `minutes` 只作 `s10` 排分集边界用。`s14` 仍报逐镜秒数合计，但**只报不拦** |

## 阶段六　第一季制作

| # | 事项 | 状态 |
| --- | --- | --- |
| 6.1 | 出图 / 出片 / 配音 / 剪辑 | ⛔ |
| 6.2 | 迭代与验收 | ⛔ |
| 6.3 | `production/` 工作目录规范 | 🔨 可先定 |

## 待你决策 ⚠

| # | 事项 |
| --- | --- |
| D1 | **第 1115~1205 章之间 41 章正文被截**（缺约 5.0 万字），影响第六季。待补章号清单见 `doc/11_待补原文清单.md`；暂按前五季推进 |
| D2 | ~~逐章分析质量达标线~~ ✅ 已定：放宽到 95%，臆造仍须为 0 |
| D3 | `doc/04` 第十四节 6 个待定项。其中 **B 画幅** ✅ 已定 16:9；**A 画风载体** 的选型矩阵已就绪（10 画面目标 × 11 画风 × 3 张），见 `doc/07_画风矩阵测试.md`，等你跑图后拍板；C 旁白处理已由 `doc/05` 解决 |
| D5 | **要不要把配色／明暗／构图从旧的 S1~S8 风格段里剥掉。** 现有几个风格段越过了单一变量原则（S6 最严重），会让矩阵结果有系统性偏差。理由与逐条清单见 `doc/12_参考片画风研究.md` 第七节。剥了才真正可比，代价是已有的选型设计要动一次 |
| D4 | ~~每集时长~~ ✅ 已定：**不做预设**（先后作废过 8-12 分、25~32 分两版规格），`doc/00` 规格表已同步 |
| D6 | ~~逐镜秒数按哪个口径重配~~ ✅ 已定：**不对时长做预设，专注内容**。`doc/00` 的 25~32 分规格作废，见 5.6 |

---

## 关键路径

```
逐章分析(2.1) ──→ 体检(2.2) ──→ 资产聚合(3.1~3.3) ──→ 剧情线(3.5,3.6)
                                                          ↓
                                    风格定稿(4.1) ──→ 资产深化(4.2,4.3)
                                                          ↓
                                    六季划分(3.7) ──→ 分集表(3.8)
                                                          ↓
                                              第一季深化(5.x) ──→ 制作(6.x)
```

**关键路径上的瓶颈换过一次。**

原先的瓶颈是 2.1（逐章分析）：它没跑完，3.5 之后的一切都无从谈起——
没有 1200 份章节数据，任何剧情线和分季方案都只能靠拍脑袋，
那会直接违反原则一（不把问题往下游传）。这一段已经过去了，
2.1~2.2 和整个阶段三都已完成。

**现在的瓶颈是 4.0 画风载体选型。** 画风一天不定，
4.2 / 4.3（角色与场景的出图提示词）就一天不能写——写了也是白写，
换个画风全部作废。所以现在能做的仍然是那两类事：

1. **把下游工具全部准备好**，让画风一定就能立刻出结果。
2. **做不依赖画风的事**——剧情线、分集、角色档案、脚本模板。

`s0_probe_text.py`、`s4_build_assets.py`、`s10_episode_plan.py`
都是按这个思路先做掉的。

## 画风未定期间能做的事（按价值排）

画风还要几天才定。这期间下面这些**一件都不依赖画风**，做完不会返工：

| # | 事 | 对应 | 为什么现在做 |
| --- | --- | --- | --- |
| 1 | ~~回填 E01/E02，写 E03~E36~~ ✅ 已完成 36 集。**接着写 E37 起** | 5.5 / 5.3a | 口径已回填干净，往下写不会返工 |
| 2 | 给全书 1200 章的 `narration` 打 N1~N5 标 | 5.5 | 打完之后每集写剧本时分类是现成的，程序可辅助。**顺带纠抽取误判**：`seq30` 十八条 `narration` 里有十一条其实是唐真的台词（见 `production/s01/E08_剧本.md` 附一）；`seq45` 七条 `narration` **全部**是老人的台词（见 `E11` 附一）；`seq61` 二十六条 `narration` 里有四条是唐真讲自己打算的整段话、二十条同时被抽取成心理活动（见 `E15` 附三）；`seq69` 六条里有四条是屏姐讲天门结构与考核标准的整段话（见 `E17` 附一）。另一个方向的误判也存在：`seq65`~`seq67` 三章正文近九千字，只抽出 16 条 `narration`，剧本里实际有 183 处 `【画】` 承载的是同样来自原文却没进 `narration` 的描写；`seq73`~`seq77` 五章一万三千字更极端，只抽出 31 条，而剧本里有 222 处 `【画】`；`seq78`~`seq82` 五章一万八千字抽出 73 条，剧本里 166 处 `【画】`，明显漏抽的有 `seq81[19]`、`seq81[44]`（夹在 43 与 45 两条 `narration` 中间被跳过）、`seq82[2]`、`seq82[47]`，心理活动也漏了 `seq81[27]`、`seq81[32]`、`seq82[40]`（见 `E20` 附一）。**E20 还查出第一例 `para` 本身错位**：`seq78[44]`「哪种火？」在原文里是第 45 行，此前十九集查到的都是分类错，这是第一次定位错——剧本段号只能以 `analysis` 为准，括号内注明原文行号，否则 `s14` 对不上账。**E21 又出一例**：`seq86` 里 `para` 记作 43 的那条（「如今在一个副峰被人拦了两剑？」）原文在第 42 行，两集连着两例，说明段号错位不是偶发。E21 另有四类新形态：同一段文字被同时判成旁白与台词（`seq83[20]`）、同时判成旁白与心理活动（`seq83[21]`）、内心话被判成 `dialogues` 且 `speaker` 写「未明说」（`seq84[39][55]`）、旁白条里裹着一句台词（`seq84[12]`）；`seq85` 五十二行只抽出 6 条 `narration`，剧本里 162 处 `【画】` 大半来自那一章（见 `E21` 附一）。**E22 又添两类**：`seq87` 的 `dialogues` 里唐真的「啊？」重复了一条（同章第 39/47/48 行各有两句引语，抽取把「同一行的第二句」补在列表末尾时，把只有一句的第 40 行也带了一遍），这是第一例重复条目；「同时进 `narration` 与 `monologues`」在 `seq88[50]`、`seq88[62]` 再现两例，**这一类比 `para` 错位更常见**。抽取密度失衡也更极端：`seq87` 五十三行只有 4 条 `narration`，而 `seq88` 七十二行有 34 条 `monologues`（并列全书至此单章第二多，第一是 `seq61` 的 56 条）（见 `E22` 附一）。**E23 又添三类**：`narration` 条里裹着整句台词而同一句又进了 `dialogues`（`seq91[117]`，与 E22 的 `seq84[12]` 同类）；抽取给一段**假设**对话发明了说话人（`seq91[167][169][171]` 三条同时进 `narration` 与 `dialogues`，`speaker` 写作「心魔（假设）」）；**抽取自己的 `function` 与所在列表打架**——`seq91[202]` 在 `narration` 里而 `function` 写「心理活动」，第一例。反方向漏抽也出现在最显眼的位置：`seq91` 开篇四行（「胜，但胜之不武。」起）全部不在 `narration` 里。另记 `seq91` 是全书至此最大的一章：210 行、93 条 `narration`，分别是第二名（`seq38` 的 108 行、`seq37` 的 47 条）的两倍（见 `E23` 附一）。**E24 又添三类，其中两类是说话人被指错**：`seq93[54][56]` 的 `speaker` 都写「唐真」，而原文第 55 行当场否认（「这话是对面的自己开口说的」）——**这是全集最重要的一场戏，照抽取办整场就变成自言自语**；`seq94[35]`「狗安，你怎么样了？」的 `speaker` 写「齐渊」，而齐渊此刻正在另一个州被妖圣追着跑，原文根本没有交代是谁在敲门（与 E23 的「心魔（假设）」同类：**抽取不肯留空**）。第三类是**条目文本被从头截断**：`seq93[46]` 正文以「原来如此。”」开头，丢了前引号，说明切分点可能落在引号中间（见 `E24` 附一）。**E25 把 `para` 错位的性质弄清楚了**：`seq99` 里 `dialogues` 从 `para` 79 起偏一、`narration` 从 `para` 81 起才偏一，而夹在中间的 `narration` `para` 80 两边都对齐——**错位不是一个全局位移，是逐条产生的**，这也是第一例「同章双列表不同步」。另记一条口径：**`function` 字段从来没有闭合词表**，前九十九章里用过 26 个不同的值，包括「剑仙遗言，设定补充」「音效，砚台砸击声」这种一次性长句，`doc/05` 的 N1~N6 与它并不对应，写剧本时只能当提示不能当依据（见 `E25` 附一）。**E26 又添两类**：`seq101[27]` 的 `dialogues` 把**两段引语加中间那句叙述**并成了一条，只剥掉最外层的一对引号——与 E24 `seq93[46]` 的「从头截断」是同一台机器的两种失误，**切分点落在引号里**；这一类会让 `s14` 的逐字覆盖检查误报，**属于可放过的误报**。另一类是 `seq100[16]` 的 `monologues` 正文吞掉了上一行，而那一整句正是 `narration` `para` 15 的全文——**同一句话同时挂在两个列表的两个条目上**，照抽取原样落镜会被判「重复承载」（见 `E26` 附一）。**E27 头一次是好消息**：`seq104[2]` 是「引语 ＋ 归属语 ＋ 引语」同处一行，抽取**拆对了**，归属语留在外面——正是 E24 `seq93[46]`、E26 `seq101[27]` 失手的那种结构，**同一台机器在同一种结构上有时对有时错**，所以逐条核对不能省；本集 `s14` 的逐字覆盖检查一条都没报。抽取密度失衡继续：`seq105` 六十四行只有 3 条 `narration`，`seq106` 八十四行只有 6 条，而这两章正是全集人最多、走位最密的两场（见 `E27` 附一）。**E28 查出一对互为镜像的新问题，而且在同一章**：`seq108[17]` 的 `dialogues` 里有一条**凭空生成的台词正文**（「姚安饶：为什么？」），原文那一行整行没有引号也没有这三个字——这是全书至此第一例正文被造出来，与 E23 的「心魔（假设）」、E24 的「齐渊」（给真实台词安错说话人）性质不同；而同一章 `seq108[37]` 里**真实存在**的那句「为什么？」却被漏掉了。**一个词，一章之内，一句造出来一句丢掉**——`dialogues` 既不是完备集也不是可信集。另记 `seq110` 是全书至此**第一章 `monologues` 为空的正文章节**（55 行 / 23 条 `narration` / 0 条 `monologues`），这一次抽取没错，那一章确实全是外部视角的编年记述（见 `E28` 附一）。**E29 把段号错位这一类又推进了一步**：`seq116` 的 `narration` 第 26 条正文其实是**第 27 行**，而同一行号在 `monologues` 里记的 26 **是对的**——**同一个 para，一个列表错一个列表对**，这是继 E20 `seq78[44]`、E21 `seq86[43]`、E25 `seq99` 之后的第四例，也是第一例这种形态。另有一例张冠李戴：`seq115` 的 `monologues` 记 `para` 32，正文却是**第 31 行的全文**，而第 31 行同时还有一条正文一模一样的 `narration`；**原文第 32 行的真正内容两个列表里都没有**。此外 `seq112[53]`（胡九）与 `seq114[14]`（吕藏锋）两条现场台词被同时记进 `narration`，剧本用 `【台】` 处置——**这是 E21 以来第一次用到这个标记**（见 `E29` 附一）。**E30 又添第五例段号错位**：`seq120` 的 `monologues` 记 `para` 39，正文却是第 40 行，而第 39 行的真正内容两个列表里都没有。同章另有两类：`seq117[30][32][33]` **三条同时进 `narration` 与 `monologues`**（一章连出三条是第一次）；**同一件事在相邻两章被安了两个说话人**——`seq119[46][57][65]` 三声唱名记作「司仪」且同时进 `narration`，`seq120[11][24][33]` 三声唱名记作「唱名声」且只在 `dialogues` 里，剧本对前者用 `【台】`、对后者按普通台词落镜（见 `E30` 附一）。**E31 给「`function` 没有闭合词表」这条口径又添一例**：`seq123[34]` 的正文只有 `。。。`，`function` 却写作「**过渡**」——全书至此 15 条纯省略号 `narration` 里，前 14 条一律记「时间推移」，只有这一条例外；它是 `function` 的第 28 个取值（`seq1~126` 统计）。同章另有：`seq124[40]`「又比如金童峰的月亮。」同时进 `narration` 与 `dialogues`（用 `【台】`）；**同一个人同一集两个名字**——`seq122` 四处写「杜有才」，`seq126[22]` 写「杜有为」（见 `E31` 附一）。**E32 把幕间省略号这一类彻底钉死了**：`seq128[40]` 又是一行只有 `。。。` 的幕间记号，但这一条**哪张表都没进**——与 E31 `seq123[34]`（进了 `narration`、`function` 写「过渡」）隔七章形成对照，**同一种记号进不进 `narration` 完全随机**，剧本一律自己落镜，不依赖抽取。同集另有两类：`seq127[4]` 的 `dialogues` 把一句**弦外之音**记在听者名下（原文「金浩却听出了其中弦外之音，『某要让对方适应境界，抓紧出击！』」，说话的是那声高喊，不是金浩）——这是继 E23「心魔（假设）」、E24「齐渊」之后**第三种说话人指错的形态：把「听者」当成「说者」**；以及 `seq127` 有三行（[17][18][41]）**完全不在任何一张表里**，其中 [41]「没有什么能阻挡对方。」是全场的判词，剧本用字幕卡落镜。另记 `seq130` 是前 130 章里单章 `narration` 第三多的一章（49 条，前两名是 `seq91` 的 93 条、`seq95` 的 60 条），其中 23 条 `function` 写「世界观设定」，整章是《罗生门精解》的说明书，**导致 E32 的唐假承载比达到 68/77 = 88%，全季最高**（见 `E32` 附一）。**E33 查出全季最重的一次说话人指错**：`seq135[81][86][88]` 三句台词的 `speaker` 全部记作「红儿」，而这三句是**姚安饶**的——同集四条证据可查（`seq131[12]` 红儿在观里榕树下、`seq131[16]` 姚安饶提着老剑借了颗暗淡夜明珠钻进地洞、`seq134[45]` 唐真想的仍是「榕树下沉默修炼的红儿」、`seq135[83][84]` 来人「一手暗淡夜明珠一手旧剑从地下水脉爬出」三样全对上）。**这是第一例把主角级角色指错的案例**；原文自己从头到尾只写「一个如鬼一样的女人」「女鬼」，剧本因此也只写「女鬼」，两边都不采信。同集另有四类：`seq132[23][52]` 两整句台词同时进 `narration` 与 `dialogues` 且 `function` 都写「伏笔」（用 `【台】`）；`seq133[13][17]` 两条 `dialogues` 把归属语并进台词正文（与 E24 `seq93[46]`、E26 `seq101[27]` 同类）；`seq132[33]` 同一句现场台词同时挂在 `monologues` 与 `dialogues`（与 E26 `seq100[16]` 同类）；`seq135[20][30]` 的 `speaker` 写「未明说」、`[66]` 写「阵外之人」，而原文**下一行就点破了是魏成、是金桧**——与 E21 `seq84[39][55]` 同类，**抽取宁可写「未明说」也不肯往后读一行**（见 `E33` 附一）。**E34 把「`narration` 不是完备集」这条推到了极端**：`seq140` 六十六行只有 4 条 `narration`（全是《水经注》的说明），而整集的收束句 `seq140[53]`「地底升起了太阳。」**哪张表都没进**；同类的还有 `seq139[59]`「是——鲶鱼？」、`seq139[50][51]`、`seq140[36][37][38]`、`seq136[24][25]`。同集另有五类：`seq136[49]`（玉蟾宫已空的那条情报）与 `seq137[99]`「佛影？！」**同时进 `narration` 与 `dialogues`**（均用 `【台】`）；`seq136[47]` 与 `seq137[99]` 的 `speaker` 都写「未明说」而原文当场点名（本集第二、三例）；`seq138[20]` 的 `monologues` 一口气吞掉三行、其中两行本身就是独立 `narration`（与 E26 `seq100[16]` 同类）；`seq136` 是全书第二章 `monologues` 为空的正文章节（第一章是 `seq110`），而这一次抽取**判错了**——`seq136[12]~[17]` 六条连续的原理说明显然是唐真跑动中的盘算，却全进了 `narration`；以及 `seq137[69]` 把「金桧」写成「**金辉**」，与 E31 的「杜有才／杜有为」同类（见 `E34` 附一）。**E35 出第六例 `para` 错位**：`seq141[48]` 的正文「她站起身，开始在地上寻找……」原文实为第 50 行（前五例是 E20 `seq78[44]`、E21 `seq86[43]`、E25 `seq99`、E29 `seq116[26]`、E30 `seq120[39]`）。同集另有两类：`seq143[58]` 与 `seq143[77]` 的 `speaker` 都写「未明说」，而两句都是**同一行里紧跟在杜有才自己上半句后面的第二句引语**——**这不是「不知道是谁」，是「同一行的第二句引语没有继承说话人」**，与 E33 `seq135[20][30]`、E34 `seq136[47]`、`seq137[99]` 同类，**四集连出，已成常态**；以及 `seq143[73][74][75]`「玩笑，玩笑。／如果我笑了，就是玩。／如果不笑，也是完。」三行哪张表都没进（与 E34 `seq140[53]` 同类），同类未进表的还有 `seq141[42]~[46]`、`seq141[20]`、`seq144[26]`、`seq145[41][42]`（见 `E35` 附一）。**E36 把幕间省略号这一类凑齐了第三种待遇**：`seq149[33]` 又是一行只有 `。。。` 的幕间记号，这一条**进了 `narration`**，`function` 写「情节交代」——而 E31 `seq123[34]` 也进了 `narration` 但 `function` 写「过渡」，E33 `seq128[40]` 则**哪张表都没进**。**同一种记号，三章三种归类**。同集另有两类：`seq148[37]` 与 `seq148[38]` **同时进 `narration` 与 `monologues` 且正文一字不差**（与 E26 `seq100[16]`、E33 `seq132[33]` 同类）；`seq149[43]` 一句话同时踩中两类错——`dialogues` 把「天仙境。」的 `speaker` 写成「未明说」（而原文同一行下半句就写了是周东东），`monologues` 那一条的正文则把上一句的收尾引号也吞了进来（「天仙境。”周东东扫视着场间……」，与 E24 `seq93[46]`、E26 `seq101[27]` 的「切分点落在引号里」同类）（见 `E36` 附一） |
| 3 | **补齐第一季主要角色的深度档案** | 3.12 → 4.2 | 25 个主要角色只有 14 个有档案。这是 4.2 的前置，画风定了就只剩「加风格段」 |
| 4 | 角色/场景资产卡的 **`【原】` 取证层**（原文怎么写的），风格段留空 | 4.2 / 4.3 | 同上。取证与画风无关，占 4.2 工作量的大头 |
| 5 | 修 `s11` 的角色归属（换成 `s13.build_resolver`） | 3.11 | 几分钟的事，修完 `episode_assets.json` 的角色列才可信 |
| 6 | 消化 `season_roster.md` 的 A/B 两类数据问题 | 3.11 | A 是 30 对疑似同人异名，B 是 7 个把地点当成人。补 `s8` 的 PRESETS 即可 |
| 7 | 卷段划分人工重做（余下五块） | 3.10 / P1 | 与画风无关，`arcs_manual_p02.md` 是现成模板 |
| 8 | `doc/04` 待定项 D 节奏 / E 分层节奏 / F 配音 | 4.1 | 三项都不依赖画风 |
| 9 | `doc/04` 12.3 分季色彩 | 4.1 | 剧情线已出，且 `doc/12` 里有姜子牙的分场色彩做法可抄 |
| 10 | 章级伏笔对照表（2400 埋 / 2464 收） | 3.6 | 纯数据活 |

**建议主线是 1 → 2 → 3/4 并行**，5 和 6 顺手做掉。

## 文档索引

| 想知道什么 | 看哪份 |
| --- | --- |
| 接手项目 / 换会话 | `doc/10_交接说明.md` |
| 某个文件、某个 JSON 字段是什么意思 | `doc/09_数据字典.md` |
| 目录结构与文档导航 | `README.md` |
| 跑脚本、排查跑批故障 | `pipeline/README.md` |
