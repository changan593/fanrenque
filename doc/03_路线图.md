# 路线图与阻塞状态

全部待办一次列清，并标明**哪些卡在逐章分析上、哪些不卡**。

状态记号：✅ 已完成 · 🔧 工具就绪待数据 · ⏳ 进行中 · ⛔ 被阻塞 · ⚠ 待你决策

---

## 阶段一　原文标准化 ✅

| # | 事项 | 状态 |
| --- | --- | --- |
| 1.1 | txt → `source/novel.json`（1200 个分析单元） | ✅ |
| 1.2 | 章节编号异常排查（重号/跳号/合并单元） | ✅ |
| 1.3 | 数据缺陷成文 `doc/01` | ✅ |

## 阶段二　逐章独立分析 ✅

| # | 事项 | 状态 |
| --- | --- | --- |
| 2.0 | **修复思考模式默认开启导致的批量失败**（500 章只成功 9 章） | ✅ `LLM_THINKING=0` + 自适应降级重试 |
| 2.1 | 跑全书 1200 章（本地 deepseek-v4-flash） | ✅ 1200/1200 完成 |
| 2.2 | `s3_validate_chapters.py` 全量体检 | ✅ 逐字 100% / 台词覆盖 100% / 0 问题章 |
| 2.3 | 人工抽检，逐字比对原文 | ✅ 全书 566 条修复片段全量复核：0 条非原文子串、0 条缩水超 30% |
| 2.4 | `--phase review --force` 补一轮完整上下文审查 | ⚠ 未做，需花一次审查的钱；当前分数已达标，性价比低 |
| 2.5 | ~~不达标章节重跑~~ | ✅ 改为 `s5_repair_quotes.py` 程序化对齐修复，未花 API |

## 阶段三　资产与结构规划 ✅（3.6 / 3.10 除外）

| # | 事项 | 状态 |
| --- | --- | --- |
| 3.1 | 角色资产聚合 | ✅ 973 角色 |
| 3.2 | 场景资产聚合 | ✅ 1364 场景 |
| 3.3 | 角色同框关系网 | ✅ 3000 对 |
| 3.4 | 纯原文结构探查（角色池雏形、篇幅曲线、卷段边界候选） | ✅ `s0_probe_text.py` 已跑出 |
| 3.5 | 全书剧情线 | ✅ `doc/08`：地理六幕、阵容曲线、十一条长线 |
| 3.6 | 伏笔埋设与回收对照表 | 🔧 `threads.json` 已出 11 条长线；章级 2400 条待细排 |
| 3.7 | 六季划分方案 | ✅ 47/46/42/46/41/46 集，极差 5 |
| 3.8 | 分集表 | ✅ 268 集，`data/plot/episodes.json` |
| 3.9 | 分集规划脚本 | ✅ `s10_episode_plan.py` 最短路求解 |
| 3.10 | 卷段检测 | ⚠ `s12_detect_arcs.py` 算法版已出 88 段，但只六成站得住，仅作软约束；建议额度恢复后人工重做。另：原文自带的两处卷末记号（ch62、ch254）都落在集的正中间，已决定不为它们调整分集边界，第一处按原样做成字幕卡保留在 `E16` 第 81 镜，理由见 `E16` 附二 |
| 3.11 | 每集资产需求表 | ⚠ `s11_episode_assets.py` → `episode_assets.json`。场景列可用，**角色列归属有缺陷**（把 `红儿` 算成 `师姐` 而非 `姚望舒`），见 `doc/10` 5.6 |
| 3.13 | 按季资产清单 | ✅ `s13_season_roster.py` → `season_roster.json/.md`。归属判定分三级，是 4.2/4.3 的工作队列 |
| 3.12 | 主要角色深度档案 | ✅ 14 份，`s8_character_dossier.py` 取证后逐字撰写 |

## 阶段四　风格定稿与资产深化 ⏳ ← 当前

| # | 事项 | 状态 |
| --- | --- | --- |
| 4.0 | **画风载体选型** | 🔴 **全项目唯一阻塞点**，选型矩阵已就绪，`s6` → `s7` 跑完拍板 |
| 4.1 | 风格规范：画风、人物、动作、打斗、对话、场景、观感、转场、特效 | ✅ 草案已出 `doc/04`，含 6 个待你拍板的待定项 |
| 4.2 | 角色资产 → 可直接出图的超详细提示词 | ⛔ 需 3.1 + 4.1。**工作队列已就绪**：第一季 203 角色（主要 25 / 次要 31 / 龙套 147），见 `data/plot/season_roster.md` |
| 4.3 | 场景资产 → 同上 | ⛔ 需 3.2 + 4.1。同上：第一季 274 场景，其中 ≥3 章的 22 个是重点 |
| 4.4 | 跨集一致性约束文档（锁定项清单） | 🔨 框架可先写 |
| 4.5 | 一致性检查工具（境界/时间线/称呼漂移） | 🔨 可先写 |

## 阶段五　第一季深化

| # | 事项 | 状态 |
| --- | --- | --- |
| 5.1 | 第一季剧情线细化 | 🔨 **不阻塞**，`data/plot/digest_p01.md` 就是原材料 |
| 5.2 | 每集详细安排（2~5 章一集） | ✅ `s10` 已求解，47 集，见 `episodes.json`。其 `minutes` 只是求解器排边界用的估值，非验收标准（见 5.6） |
| 5.3a | 分镜脚本 · **声音层**（台词/独白/旁白分派、镜号、景别、时长、表演提示） | ⏳ **E01~E24 完成**（24/47），五道闸门全绿。E25 起继续 |
| 5.3b | 分镜脚本 · **画面提示词层** | ⛔ 需 4.0 画风 |
| 5.4 | 剧集脚本的数据结构与模板 | ✅ 试点已定型，见 `production/s01/E01_剧本.md` |
| 5.5 | **旁白六类分派**（N1~N6）与承载账 | ✅ 规范已定（`doc/05` 第五节），闸门就绪（`s14`），E01~E19 全部合规。世界静止：E01 5、E02 6、E03 3、E04 4、E05 3、E06 3、E07 3、E08 3、E09 3、E10 4、E11 4、E12 3、E13 4、E14 5、E15 5、E16 4、E17 5、E18 3、E19 5、E20 **6（首次用满上限）**、E21 5、E22 5、E23 **6（第二次用满）**、E24 **6（第三次用满）**（均 ≤6）。`s14` 又补了两道机械检查：**跨 seq 撞段号**（段号只记 `para` 不记 `seq`，E13 镜083 是 seq51[35] 却差点算成 seq50[35] 的拆条，现在按镜头正文回查它属于哪一章）与**逐字回查全部十三集**（查出 E04/E05/E06/E09 各有一处 `【画】` 把原文的人名换成了代词，已改回）。两项都进了 `selftest` 第 12 节。E14 又补了第三道：**附表不参与比对**（附表行的首格也是三位数字，正文里再出现一个 `【白】` 就会被当成真标记，E14 的附四正是这样多出过一条没写 `※` 的白描留声）。这一改顺带查出 E01 镜179、E02 镜013 两处归属句只写在表演提示栏里，已挪回声音栏。E16 又补了第四道：**镜头归章按前后邻居收窄与补空**（`【删】。。。[39]` 这种镜头正文认不出属于哪一章，两章都有第 39 段就会互相撞；镜头是按叙事顺序排的，前后两镜同属一章就够判了）。E17 又补了第五道：**正向包含也要排除「整条正文就是另一条旁白」**（seq68[49]「朽木不可雕也。」整个嵌在 seq68[57]「实在是朽木不可雕也。」里，此前反向包含挡得住、正向包含挡不住） |
| 5.6 | ~~逐镜秒数重配~~ | ✅ **已定：不对时长做预设，专注内容。** 十九集实测 19:41 / 25:33 / 25:40 / 41:29 / 38:38 / 48:34 / 54:14 / 40:37 / 54:58 / 61:37 / 67:01 / 55:18 / 61:53 / 61:24 / 40:40 / 57:50 / 73:23 / 46:34 / 81:36 / 79:28 / 60:57 / 65:00 / 72:32 / 70:40，散得很开——因为「一集多长」是内容量的结果，不是可以先定后写的指标。`doc/00` 的 25~32 分规格随之作废，`episodes.json` 的 `minutes` 只作 `s10` 排分集边界用。`s14` 仍报逐镜秒数合计，但**只报不拦** |

## 阶段六　第一季制作

| # | 事项 | 状态 |
| --- | --- | --- |
| 6.1 | 出图 / 出片 / 配音 / 剪辑 | ⛔ |
| 6.2 | 迭代与验收 | ⛔ |
| 6.3 | `production/` 工作目录规范 | 🔨 可先定 |

## 待你决策 ⚠

| # | 事项 |
| --- | --- |
| D1 | **第 1115~1205 章之间 41 章正文被截**（缺约 5.0 万字），影响第六季。待补章号清单见 `doc/11_待补原文清单.md`；暂按前五季推进 |
| D2 | ~~逐章分析质量达标线~~ ✅ 已定：放宽到 95%，臆造仍须为 0 |
| D3 | `doc/04` 第十四节 6 个待定项。其中 **B 画幅** ✅ 已定 16:9；**A 画风载体** 的选型矩阵已就绪（10 画面目标 × 11 画风 × 3 张），见 `doc/07_画风矩阵测试.md`，等你跑图后拍板；C 旁白处理已由 `doc/05` 解决 |
| D5 | **要不要把配色／明暗／构图从旧的 S1~S8 风格段里剥掉。** 现有几个风格段越过了单一变量原则（S6 最严重），会让矩阵结果有系统性偏差。理由与逐条清单见 `doc/12_参考片画风研究.md` 第七节。剥了才真正可比，代价是已有的选型设计要动一次 |
| D4 | ~~每集时长~~ ✅ 已定：**不做预设**（先后作废过 8-12 分、25~32 分两版规格），`doc/00` 规格表已同步 |
| D6 | ~~逐镜秒数按哪个口径重配~~ ✅ 已定：**不对时长做预设，专注内容**。`doc/00` 的 25~32 分规格作废，见 5.6 |

---

## 关键路径

```
逐章分析(2.1) ──→ 体检(2.2) ──→ 资产聚合(3.1~3.3) ──→ 剧情线(3.5,3.6)
                                                          ↓
                                    风格定稿(4.1) ──→ 资产深化(4.2,4.3)
                                                          ↓
                                    六季划分(3.7) ──→ 分集表(3.8)
                                                          ↓
                                              第一季深化(5.x) ──→ 制作(6.x)
```

**关键路径上的瓶颈换过一次。**

原先的瓶颈是 2.1（逐章分析）：它没跑完，3.5 之后的一切都无从谈起——
没有 1200 份章节数据，任何剧情线和分季方案都只能靠拍脑袋，
那会直接违反原则一（不把问题往下游传）。这一段已经过去了，
2.1~2.2 和整个阶段三都已完成。

**现在的瓶颈是 4.0 画风载体选型。** 画风一天不定，
4.2 / 4.3（角色与场景的出图提示词）就一天不能写——写了也是白写，
换个画风全部作废。所以现在能做的仍然是那两类事：

1. **把下游工具全部准备好**，让画风一定就能立刻出结果。
2. **做不依赖画风的事**——剧情线、分集、角色档案、脚本模板。

`s0_probe_text.py`、`s4_build_assets.py`、`s10_episode_plan.py`
都是按这个思路先做掉的。

## 画风未定期间能做的事（按价值排）

画风还要几天才定。这期间下面这些**一件都不依赖画风**，做完不会返工：

| # | 事 | 对应 | 为什么现在做 |
| --- | --- | --- | --- |
| 1 | ~~回填 E01/E02，写 E03~E24~~ ✅ 已完成 24 集。**接着写 E25 起** | 5.5 / 5.3a | 口径已回填干净，往下写不会返工 |
| 2 | 给全书 1200 章的 `narration` 打 N1~N5 标 | 5.5 | 打完之后每集写剧本时分类是现成的，程序可辅助。**顺带纠抽取误判**：`seq30` 十八条 `narration` 里有十一条其实是唐真的台词（见 `production/s01/E08_剧本.md` 附一）；`seq45` 七条 `narration` **全部**是老人的台词（见 `E11` 附一）；`seq61` 二十六条 `narration` 里有四条是唐真讲自己打算的整段话、二十条同时被抽取成心理活动（见 `E15` 附三）；`seq69` 六条里有四条是屏姐讲天门结构与考核标准的整段话（见 `E17` 附一）。另一个方向的误判也存在：`seq65`~`seq67` 三章正文近九千字，只抽出 16 条 `narration`，剧本里实际有 183 处 `【画】` 承载的是同样来自原文却没进 `narration` 的描写；`seq73`~`seq77` 五章一万三千字更极端，只抽出 31 条，而剧本里有 222 处 `【画】`；`seq78`~`seq82` 五章一万八千字抽出 73 条，剧本里 166 处 `【画】`，明显漏抽的有 `seq81[19]`、`seq81[44]`（夹在 43 与 45 两条 `narration` 中间被跳过）、`seq82[2]`、`seq82[47]`，心理活动也漏了 `seq81[27]`、`seq81[32]`、`seq82[40]`（见 `E20` 附一）。**E20 还查出第一例 `para` 本身错位**：`seq78[44]`「哪种火？」在原文里是第 45 行，此前十九集查到的都是分类错，这是第一次定位错——剧本段号只能以 `analysis` 为准，括号内注明原文行号，否则 `s14` 对不上账。**E21 又出一例**：`seq86` 里 `para` 记作 43 的那条（「如今在一个副峰被人拦了两剑？」）原文在第 42 行，两集连着两例，说明段号错位不是偶发。E21 另有四类新形态：同一段文字被同时判成旁白与台词（`seq83[20]`）、同时判成旁白与心理活动（`seq83[21]`）、内心话被判成 `dialogues` 且 `speaker` 写「未明说」（`seq84[39][55]`）、旁白条里裹着一句台词（`seq84[12]`）；`seq85` 五十二行只抽出 6 条 `narration`，剧本里 162 处 `【画】` 大半来自那一章（见 `E21` 附一）。**E22 又添两类**：`seq87` 的 `dialogues` 里唐真的「啊？」重复了一条（同章第 39/47/48 行各有两句引语，抽取把「同一行的第二句」补在列表末尾时，把只有一句的第 40 行也带了一遍），这是第一例重复条目；「同时进 `narration` 与 `monologues`」在 `seq88[50]`、`seq88[62]` 再现两例，**这一类比 `para` 错位更常见**。抽取密度失衡也更极端：`seq87` 五十三行只有 4 条 `narration`，而 `seq88` 七十二行有 34 条 `monologues`（并列全书至此单章第二多，第一是 `seq61` 的 56 条）（见 `E22` 附一）。**E23 又添三类**：`narration` 条里裹着整句台词而同一句又进了 `dialogues`（`seq91[117]`，与 E22 的 `seq84[12]` 同类）；抽取给一段**假设**对话发明了说话人（`seq91[167][169][171]` 三条同时进 `narration` 与 `dialogues`，`speaker` 写作「心魔（假设）」）；**抽取自己的 `function` 与所在列表打架**——`seq91[202]` 在 `narration` 里而 `function` 写「心理活动」，第一例。反方向漏抽也出现在最显眼的位置：`seq91` 开篇四行（「胜，但胜之不武。」起）全部不在 `narration` 里。另记 `seq91` 是全书至此最大的一章：210 行、93 条 `narration`，分别是第二名（`seq38` 的 108 行、`seq37` 的 47 条）的两倍（见 `E23` 附一）。**E24 又添三类，其中两类是说话人被指错**：`seq93[54][56]` 的 `speaker` 都写「唐真」，而原文第 55 行当场否认（「这话是对面的自己开口说的」）——**这是全集最重要的一场戏，照抽取办整场就变成自言自语**；`seq94[35]`「狗安，你怎么样了？」的 `speaker` 写「齐渊」，而齐渊此刻正在另一个州被妖圣追着跑，原文根本没有交代是谁在敲门（与 E23 的「心魔（假设）」同类：**抽取不肯留空**）。第三类是**条目文本被从头截断**：`seq93[46]` 正文以「原来如此。”」开头，丢了前引号，说明切分点可能落在引号中间（见 `E24` 附一） |
| 3 | **补齐第一季主要角色的深度档案** | 3.12 → 4.2 | 25 个主要角色只有 14 个有档案。这是 4.2 的前置，画风定了就只剩「加风格段」 |
| 4 | 角色/场景资产卡的 **`【原】` 取证层**（原文怎么写的），风格段留空 | 4.2 / 4.3 | 同上。取证与画风无关，占 4.2 工作量的大头 |
| 5 | 修 `s11` 的角色归属（换成 `s13.build_resolver`） | 3.11 | 几分钟的事，修完 `episode_assets.json` 的角色列才可信 |
| 6 | 消化 `season_roster.md` 的 A/B 两类数据问题 | 3.11 | A 是 30 对疑似同人异名，B 是 7 个把地点当成人。补 `s8` 的 PRESETS 即可 |
| 7 | 卷段划分人工重做（余下五块） | 3.10 / P1 | 与画风无关，`arcs_manual_p02.md` 是现成模板 |
| 8 | `doc/04` 待定项 D 节奏 / E 分层节奏 / F 配音 | 4.1 | 三项都不依赖画风 |
| 9 | `doc/04` 12.3 分季色彩 | 4.1 | 剧情线已出，且 `doc/12` 里有姜子牙的分场色彩做法可抄 |
| 10 | 章级伏笔对照表（2400 埋 / 2464 收） | 3.6 | 纯数据活 |

**建议主线是 1 → 2 → 3/4 并行**，5 和 6 顺手做掉。

## 文档索引

| 想知道什么 | 看哪份 |
| --- | --- |
| 接手项目 / 换会话 | `doc/10_交接说明.md` |
| 某个文件、某个 JSON 字段是什么意思 | `doc/09_数据字典.md` |
| 目录结构与文档导航 | `README.md` |
| 跑脚本、排查跑批故障 | `pipeline/README.md` |
