# 路线图与阻塞状态

全部待办一次列清，并标明哪些被什么卡住。

状态记号：✅ 已完成 · 🔧 工具就绪待数据 · ⏳ 进行中 · ⛔ 被阻塞 · ⚠ 待你决策 · 🔨 可先做

数字以 `doc/00` 第五节为准。第一季写作中逐集查出的抽取误判、闸门演进、时长实测都在
`doc/15_写作台账.md`，本文只给结论和指针，不再把逐集记录塞进表格。

---

## 阶段一　原文标准化 ✅

| # | 事项 | 状态 |
| --- | --- | --- |
| 1.1 | txt → `source/novel.json`（1200 个分析单元） | ✅ |
| 1.2 | 章节编号异常排查（重号 / 跳号 / 合并单元） | ✅ |
| 1.3 | 数据缺陷成文 `doc/01` | ✅ |

## 阶段二　逐章独立分析 ✅

| # | 事项 | 状态 |
| --- | --- | --- |
| 2.0 | 修复思考模式默认开启导致的批量失败（500 章只成功 9 章） | ✅ `LLM_THINKING=0` + 自适应降级重试 |
| 2.1 | 跑全书 1200 章（deepseek-v4-flash） | ✅ 1200/1200 |
| 2.2 | `s3` 全量体检 | ✅ 逐字 100% / 台词覆盖 100% / 臆造 0 |
| 2.3 | 人工抽检，逐字比对原文 | ✅ 全书 566 条修复片段全量复核：0 条非原文子串、0 条缩水超 30% |
| 2.4 | `--phase review --force` 补一轮完整上下文审查 | ⚠ 未做，需花一次审查的钱；复审会在原记录上追加，不丢旧账 |
| 2.5 | 引用不逐字的章节 | ✅ 改为 `s5_repair_quotes.py` 程序化对齐修复，未花 API |
| 2.6 | 29 章审查分低于合格线（结构 < 85 或一致性 < 90，修订轮用尽） | ✅ 09-05 重审完：`--redo-failed` 过 7，`s5` 修 97 处引用后 `--phase review` 过 18，最后 4 章（ch0990 / ch1149 / ch1155 / ch1167）结构分 82、引用 100% 逐字，`s5 --waive` 人工放行，理由在各章 `quality.waiver`。`s3` 零问题 |
| 2.7 | 3 章有段号越界的引用（ch0350 / ch0670 / ch1141） | ✅ `s5` 按正文命中位置改回；ch0350 那条是只写了 `para_range` 的跨段独白，`verbatim.locate()` 记起始段 |
| 2.8 | 把 `doc/15` 第一节汇总的抽取误判（说话人不肯留空、同一句挂两张表、段号错位）回灌到 `p1_extract.txt` 与 `s5` | 🔧 材料已齐，改提示词后需在人工基准集 8 章上对照 |

## 阶段三　资产与结构规划 ✅（3.6 / 3.10 除外）

| # | 事项 | 状态 |
| --- | --- | --- |
| 3.1 | 角色资产聚合 | ✅ 997 角色。泛称（严口径）不再参与归并，「凡人」「仙师」这类词不会再把不相干的人连到唐真名下 |
| 3.2 | 场景资产聚合 | ✅ 1364 场景 |
| 3.3 | 角色同框关系网 | ✅ 3000 对 |
| 3.4 | 纯原文结构探查 | ✅ `s0_probe_text.py`，`text_probe.json` 仅作对照 |
| 3.5 | 全书剧情线 | ✅ `doc/08`：地理六幕、阵容曲线、十一条长线 |
| 3.6 | 伏笔埋设与回收对照表 | 🔧 `data/manual/threads.json` 已出 11 条长线；章级 2400 条埋 / 2464 条收待配对 |
| 3.7 | 六季划分方案 | ✅ 47 / 46 / 42 / 45 / 41 / 46 集 |
| 3.8 | 分集表 | ✅ 267 集，`data/plot/episodes.json`。**第一季 47 集的边界已随剧本锁定**，`s10` 重跑不会动它们 |
| 3.9 | 分集规划脚本 | ✅ `s10_episode_plan.py` 最短路求解 + 剧本锁 |
| 3.10 | 卷段检测 | ⚠ `s12` 算法版 90 段，只约六成站得住，仅作软约束；人工版只有 seq201~400（`data/manual/arcs_manual_p02.md`）。另：原文自带的两处卷末记号（ch62、ch254）都落在集的正中间，已决定不为它们调整分集边界，第一处按原样做成字幕卡保留在 E16 镜 081，理由见 E16 附二 |
| 3.11 | 每集资产需求表 | ✅ `s11` 已改用三级别名解析（此前「红儿算成师姐」的缺陷已修），另列 `ambiguous_characters` 供人判 |
| 3.12 | 主要角色深度档案 | ✅ 14 份（`production/characters/_深度档案/`）。⚠ 第一季 25 个主要角色只有 12 个有档案 |
| 3.13 | 按季资产清单 | ✅ `s13` → `season_roster.json/.md`，是 4.2 / 4.3 的工作队列。`has_card` 按 `production/` 卡目录里有没有母版提示词判，按**名字**匹配 |

## 阶段四　风格定稿与资产深化 ⏳ ← 进行中

| # | 事项 | 状态 |
| --- | --- | --- |
| 4.0 | 画风载体选型 | ✅ **国风三维**（`doc/04` 3.1）。落地资产 `production/style_assets/` 两份统一提示词 + 20 张场景参考图；`style_test/` 转为选型留痕 |
| 4.1 | 风格规范 `doc/04` 待定项 | A 画风 ✅ · B 画幅 ✅ 16:9 · C 旁白 ✅（`doc/05`）· **D 节奏基准 / E 分层节奏 / F 配音 ⚠ 待拍板**，不阻塞出图 |
| 4.2 | 角色资产 → 可直接出图的卡 | ⏳ 具名 15（C01~C04、C06~C09、C15~C17、C49~C52）+ 群像 5（G01~G05），E01 全覆盖。第一季 207 角色（主要 25 / 次要 29 / 龙套 153），主要优先。**PNG 已生成 15 具名 + 5 群像（唐假 Q 版待出），人工验收通过 0**；三张不过已重写提示词：C04 红儿 V6、C02 南红枝 V4（脸偏圆、缺特色）、C03 老拐子 V2（右脚未做出萎缩内翻） |
| 4.3 | 场景资产 → 同上 | ⏳ 10 个母版 + 13 张状态卡（S01~S10），E01 全覆盖，PNG 全部已生成（含平面图 SVG），人工验收通过 0。第一季 274 场景，≥ 3 章的 22 个是重点。⚠ `refs_场景参考_image2/` 题材配比与本剧相反（市井 2 张、奇观 7 张），北方小城 / 破庙 / 码头 / 院子四类参考图待补 |
| 4.4 | 跨集一致性约束 | 🔧 身份优先级链、母版 → 状态卡 → 剧情镜头的派生链、渲染锁、眉眼同源锁已落地并由 `s15` 逐字查；仍缺境界 / 时间线 / 称呼三类非视觉锁定项 |
| 4.5 | 一致性检查工具（境界 / 时间线 / 称呼漂移） | 🔨 可先写 |
| 4.6 | 画风金标准闸门 | ✅ `s15`，当前全绿 |
| 4.7 | 按幕装配出图包 | ✅ `s16` |
| 4.8 | 原文引用闸门 | ✅ `s17`，1449 条 `【原】` 引用全部逐字命中 |

## 阶段五　第一季深化 ⏳ ← 进行中

| # | 事项 | 状态 |
| --- | --- | --- |
| 5.1 | 第一季剧情线细化 | 🔨 `data/plot/digest_p01.md` 是原材料 |
| 5.2 | 每集安排 | ✅ 47 集，`episodes.json`；`minutes` 只是求解器排边界用的估值 |
| 5.3a | 分镜脚本 · **声音层** | ✅ **47/47**，485 幕、12,958 镜，逐镜秒数合计 63:49:18；47 集逐集 `s14` 五道闸门全绿。逐集的静止次数与时长见 `doc/15` 第三节 |
| 5.3b | 分镜脚本 · **画面提示词层** | ⏳ 77 幕（E01~E10）。只写画面内容不写风格，风格段由程序追加 |
| 5.3c | 分镜草图 | ⏳ 只有 E01 的 9 幕 |
| 5.4 | 剧本格式 | ✅ `doc/13_剧本格式规范.md`；抬头不再标「草稿」 |
| 5.5 | 旁白六类分派与承载账 | ✅ 规范 `doc/05` 第五节，闸门 `s14`。闸门先后补了六道机械检查，演进记录在 `doc/15` 第二节 |
| 5.6 | 逐镜秒数与时长 | ✅ **已定：不对时长做预设。** 47 集实测 19:41 ~ 162:10 |

## 阶段六　第一季制作 ⏳ ← E01 已具备开工条件，卡在卡图验收

| # | 事项 | 状态 |
| --- | --- | --- |
| 6.3 | **出图工序规范** | ✅ `doc/16_出图工序规范.md`：四类图的分工（镜头图不入库、台账入库）、依赖顺序、命名、三层验收判据、**返工决策树**、批次策略。闸门 `s18_render_ledger.py`（漏镜 / 串号 / 通过却没填版本 / 双层镜头只记一层），`--scaffold` 生成空台账；E01 已生成 217 镜次 |
| 6.1a | **出图** | ⏳ **卡在卡图验收**。E01 纸面全就绪（9 幕 212 镜提示词、具名 16 + 群像 5 角色卡、10 母版 + 13 状态场景卡），台账在 `production/s01/E01/出图台账.md`。★ 验收通过前不许开工，理由见 `doc/16` 第二节 |
| 6.1b | 出片（图 → 视频） | ⛔ **已定：先做好每一张图，视频后续再想办法**。幕文档里的「视频动态」字段原样保留 |
| 6.1c | 配音 | ⛔ 已定：**TTS 与真人都可以**，`doc/04` 待定项 F 仍需在开录前拍板 |
| 6.2 | 迭代与验收 | ⏳ 判据见 `doc/16` 第七节（身份与几何 / 内容与原文 / 风格），返工见第八节 |
| 6.4 | `production/` 工作目录规范 | ✅ 季 → 集 → 幕三级，见 `production/README.md` |

## 待你决策 ⚠

| # | 事项 |
| --- | --- |
| D1 | **第 1115~1205 章之间 41 章正文被截**（缺约 5.0 万字），影响第六季。清单见 `doc/11`；暂按前五季推进 |
| D3 | `doc/04` 第十四节余下三项：D 节奏基准 / E 分层节奏 / F 配音 |
| ~~D2~~ | ~~逐章分析质量达标线~~ ✅ 已定：逐字率 95%，臆造 = 0 不放宽，台词覆盖 90% |
| ~~D4 / D6~~ | ~~每集时长 / 逐镜秒数口径~~ ✅ 已定：不做预设 |
| ~~D5~~ | ~~要不要把配色从旧风格段里剥掉~~ 选型已结束，`style_test/` 只作留痕，不再改 |
| ~~D7~~ | ~~29 章重审要不要花这笔钱~~ ✅ 已花，09-05 做完（2.6） |

---

## 关键路径

```
逐章分析(2.1) ──→ 体检(2.2) ──→ 资产聚合(3.1~3.3) ──→ 剧情线(3.5) ──→ 六季/分集(3.7, 3.8)
                                                                          ↓
                                  画风定稿(4.0 ✅) ──→ 角色卡/场景卡(4.2, 4.3) ──→ 幕文档(5.3b) ──→ 出图(6.1)
                                                                          ↑
                                                        剧本声音层(5.3a ✅) ──┘
```

瓶颈换过两次：先是 2.1（没有 1200 份数据，一切规划只能拍脑袋），后是 4.0（画风不定，提示词写了也白写）。
两处都已过去。**现在的瓶颈是 4.2 / 4.3 的量**——每一幕出图前，它用到的角色卡和场景卡都得先有。
`season_roster.md` 按首现集排好了队，`s16 --list` 能告诉你某一幕差哪几张卡。

## 待办（按价值排）

| # | 事 | 对应 | 为什么 |
| --- | --- | --- | --- |
| 0 | **E01 的 20 张角色卡、23 张场景卡人工验收**（`doc/16` 第七节三层判据），不过的按第八节返工 | 6.1a | 通过前不许开工出图；这是唯一卡住阶段六的事 |
| 1 | 第一季主要角色的卡：25 个里已有 12 个，先补另 13 个的母版 | 4.2 | 每一幕都要用；E11 起的新角色集中在这里 |
| 2 | ≥ 3 章的 22 个重点场景的母版 | 4.3 | 同上 |
| 3 | 补齐 25 个主要角色的深度档案（现有 12） | 3.12 → 4.2 | 写卡先读卷宗再读档案，档案是卡的取证层 |
| 4 | 消化 `season_roster.md` 末尾的 A / B 两类存疑名（疑似同人异名、疑似把地点抽成人），能定的补进 `common/names.PRESETS` | 3.13 | 补一条 PRESETS，`s4` / `s9` / `s11` / `s13` 同时受益 |
| 5 | E11 起的画面提示词层 | 5.3b | 卡够了就能写 |
| 6 | 把 `doc/15` 的抽取误判回灌到提示词与 `s5` | 2.8 | 后五季跑 `s2` 前做完，少一类错就少一类人工核对 |
| 7 | 卷段人工重做（余下五块） | 3.10 | `arcs_manual_p02.md` 是模板；季边界不会变 |
| 8 | `doc/04` D / E / F 三项 | 4.1 | 不依赖画风 |
| 9 | `doc/04` 12.3 分季色彩 | 4.1 | 剧情线已出，`archive/12` 里有分场色彩的做法可抄 |
| 10 | 章级伏笔对照表（2400 埋 / 2464 收） | 3.6 | 纯数据活 |
